
Preview | Size | URL
------- | ---- | ---$each{ file in files }
[${ file.path }](![](${ file.path })) |
``` yaml
width: ${ file.dimensions.width }px
height: ${ file.dimensions.height }px
```
| [${ file.path }](${ file.path }){/}
